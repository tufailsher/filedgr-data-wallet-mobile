/// The class that contains some of the strings the app uses.
/// There are non-translatable.
class AppStrings {
  static const appName = 'FileDGR';
  static const channelNotification = 'Main Notifications';
  static const channelNotificationDescription =
      'This is the main notification channel for FileDGR';
}
